pipeline {
    agent any

    stages {
        // this is a comment
        stage('Docker') {
            steps {
                sh '''
                    docker build -t my-playwright .
                '''
            }
        }
    }
}